import { Bot, Dict, HTTP } from '@satorijs/core';
export declare class Internal {
    private bot;
    private http;
    constructor(bot: Bot, http: () => HTTP);
    static define(isGuild: boolean, routes: Dict<Partial<Record<HTTP.Method, string | string[]>>>): void;
}
export declare class GroupInternal extends Internal {
}
export declare class GuildInternal extends Internal {
}
