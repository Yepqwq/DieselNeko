/**
 * 为了未来做打算，这个模块是必须的，因为最终插件都要和数据库打交道的。
 */
import { Session } from "koishi";
export declare function setDefaultConfig(config: Config): void;
export declare function getConfig(session?: Session): Config;
export declare function getNickname(session: Session<'name'>): string;
export declare function getGreatSuccessNum(session: Session): number;
