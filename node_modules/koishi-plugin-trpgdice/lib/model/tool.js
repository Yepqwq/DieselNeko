"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getUserCard = void 0;
async function getUserCard(session) {
    if (session.guild) {
        if (session.platform == "onebot") {
            let bot = session.bot; // as OneBotBot
            const info = await bot.internal?.getGroupMemberInfo(session.guildId, session.userId);
            if (info)
                return info.card;
        }
        return session.username;
    }
}
exports.getUserCard = getUserCard;
