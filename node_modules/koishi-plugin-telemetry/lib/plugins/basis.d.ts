import type { Context, HTTP } from 'koishi';
import type { Root } from '../types';
export interface Hello {
    ver: 1;
    privacyVer: number;
    alerts: HelloAlert[];
}
export interface HelloAlert {
    id: number;
    title: string;
    content: string;
}
export declare class TelemetryBasis {
    #private;
    root: Root;
    constructor(ctx: Context, root: Root);
    private init;
    private http;
    hello: Hello;
    private ready;
    whenReady: () => Promise<boolean>;
    post: HTTP.Request2;
}
