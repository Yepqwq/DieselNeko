/// <reference types="node" />
import { FileHandle } from 'fs/promises';
import { Logger } from 'koishi';
export declare class FileWriter {
    date: string;
    path: string;
    data: Logger.Record[];
    task: Promise<FileHandle>;
    size: number;
    private temp;
    constructor(date: string, path: string);
    flush(): void;
    parse(text: string): any[];
    read(): Promise<Logger.Record[]>;
    write(record: Logger.Record): void;
    close(): Promise<void>;
}
